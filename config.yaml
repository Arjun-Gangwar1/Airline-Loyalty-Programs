# Project Configuration
# ======================

PROJECT:
  name: "Airline Loyalty Behavioral Intelligence"
  version: "1.0.0"
  author: "Your Name"

DATA:
  raw_path: "data/raw/"
  processed_path: "data/processed/"
  final_path: "data/final/"
  
PREDICTION:
  prediction_date: "2017-12-31"
  churn_definition: "combined"  # hard, activity, engagement, combined
  activity_threshold_months: 12
  
FEATURES:
  lookback_months: 6
  
MODELS:
  random_state: 42
  test_size: 0.2
  
OUTPUTS:
  models_path: "outputs/models/"
  figures_path: "outputs/figures/"
  reports_path: "outputs/reports/"

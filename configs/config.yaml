project:
  name: airline_behavioral_intelligence
  version: 1.0

data:
  raw_path: data/raw/
  processed_path: data/processed/
  final_path: data/final/

model:
  random_state: 42
  test_size: 0.2

features:
  prediction_horizon_months: 3
  inactivity_threshold_months: 12

outputs:
  models: outputs/models/
  figures: outputs/figures/
  reports: outputs/reports/